parameters:
  appName: ''
  scheme: ''

stages:
- stage: iOS_Build
  jobs:
  - job: Build
    pool:
      vmImage: macos-latest
    steps:
    - template: ../build/ios-build.yml
      parameters:
        appName: ${{ parameters.appName }}
        scheme: ${{ parameters.scheme }}
